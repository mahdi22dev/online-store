import SectionTitle from "@/components/text/SectionTitle";

export default async function Refunds() {
  return (
    <main className="min-h-screen pt-1">
      <SectionTitle text="refunds" />
    </main>
  );
}
